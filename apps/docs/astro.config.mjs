import { defineConfig } from 'astro/config';
import starlight from '@astrojs/starlight';
import react from '@astrojs/react';

export default defineConfig({
  // Placeholder; update when a host/domain is chosen. No `base` — served at root.
  site: 'https://mycl.dev',
  integrations: [
    react(),
    starlight({
      title: 'mycl',
      // Self-contained lockup (mark + outlined Inter wordmark), one variant per theme.
      // The mark-only logo.svg remains the hero image.
      logo: {
        light: './src/assets/lockup-light.svg',
        dark: './src/assets/lockup-dark.svg',
        replacesTitle: true,
      },
      favicon: '/favicon.svg',
      tagline: 'connect your code',
      // No GitHub entry: the star badge in the SocialIcons override carries
      // GitHub's own logo and links to the same repo, so the built-in icon was
      // a second GitHub link sitting next to the first. Any social entry added
      // here still renders, ahead of the badge.
      social: [],
      customCss: ['./src/styles/custom.css'],
      // Overrides for the site-wide docs-sidebar collapse:
      //  - Sidebar: prepends the collapse toggle (top-right of the sidebar).
      //  - SocialIcons: renders the built-in social icons, the collapsed-state rail,
      //    and hosts the shared collapse click handler + localStorage persistence.
      //  - Header: wraps the default header with the always-on early-development
      //    notice, a full-width bar above the nav row. See src/overrides/Header.astro;
      //    the layout room for it is made in custom.css.
      components: {
        Sidebar: './src/overrides/Sidebar.astro',
        SocialIcons: './src/overrides/SocialIcons.astro',
        Header: './src/overrides/Header.astro',
      },
      head: [
        { tag: 'link', attrs: { rel: 'preconnect', href: 'https://fonts.googleapis.com' } },
        { tag: 'link', attrs: { rel: 'preconnect', href: 'https://fonts.gstatic.com', crossorigin: true } },
        { tag: 'link', attrs: { rel: 'stylesheet', href: 'https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap' } },
        // Apply the persisted docs-sidebar collapse before first paint (no flash).
        // Shares the localStorage key with the header toggle in SocialIcons.astro.
        {
          tag: 'script',
          content:
            "try{if(localStorage.getItem('mycl:sidebar-collapsed'))document.documentElement.setAttribute('data-sidebar-collapsed','')}catch(e){}",
        },
      ],
      sidebar: [
        {
          label: 'Getting Started',
          items: ['getting-started/introduction', 'getting-started/installation', 'getting-started/quick-start'],
        },
        {
          label: 'Concepts',
          items: [
            'guide/channels', 'guide/capabilities', 'guide/scopes', 'guide/registries-and-layers',
            'guide/factories', 'guide/augmentation', 'guide/layer-strategies', 'guide/snapshots-and-async',
          ],
        },
        {
          label: 'Advanced',
          items: [
            'advanced/core-substrate', 'advanced/building-a-kernel', 'advanced/type-guarantees',
            'advanced/duplicate-detection', 'advanced/concepts',
          ],
        },
        // Generated by scripts/generate-recipe-pages.mjs from src/recipes/manifest.ts.
        { label: 'Recipes', items: [{ autogenerate: { directory: 'recipes' } }] },
        {
          label: 'Reference',
          // reference/changelog is generated by scripts/generate-changelog-page.mjs
          // from packages/core/CHANGELOG.md.
          items: ['reference/core', 'reference/glossary', 'reference/changelog'],
        },
        // Generated by scripts/generate-error-pages.mjs from core's MESSAGES table.
        { label: 'Error reference', collapsed: true, items: [{ autogenerate: { directory: 'errors' } }] },
        {
          label: 'For Agents',
          items: ['agents/using-mycl'],
        },
      ],
    }),
  ],
});
